# 删除滤波代码记录

**日期**: 2024年12月17日
**文件**: `get_pose_indemind_left.cpp`
**操作**: 删除所有双边滤波(Bilateral Filter)相关代码

---

## 1. 删除原因

根据用户需求，移除项目中的双边滤波功能。

---

## 2. 原滤波功能说明

### 2.1 滤波类型
- **双边滤波 (Bilateral Filter)**: OpenCV `cv::bilateralFilter()` 函数

### 2.2 滤波参数
| 参数 | 值 | 说明 |
|------|-----|------|
| kernel_size | 5 | 滤波窗口大小 (3-7 推荐) |
| sigmaColor | 40 | 颜色空间标准差 (30-50 适用于深度) |
| sigmaSpace | 40 | 坐标空间标准差 (30-50) |

### 2.3 原滤波代码
```cpp
// Apply bilateral filtering to depth data for noise reduction
// This filtering operates in spatial domain only (no time delay)
// Perfect for high-speed motion scenarios (trampoline athletes, etc.)
if (!depth_data.empty() && enable_bilateral_filter) {
    // Convert depth to 32F format for bilateral filtering (which only supports 8U and 32F)
    cv::Mat depth_float;
    depth_data.convertTo(depth_float, CV_32F);

    // Apply bilateral filter
    cv::Mat depth_filtered;
    // Parameters: kernel_size=5, sigmaColor=40, sigmaSpace=40
    // - kernel_size: filter window size (3-7 recommended)
    // - sigmaColor: color space standard deviation (30-50 for depth)
    // - sigmaSpace: coordinate space standard deviation (30-50)
    cv::bilateralFilter(depth_float, depth_filtered, 5, 40, 40);

    // Convert back to 16U format (millimeters)
    depth_filtered.convertTo(depth_data, CV_16U);
}
```

### 2.4 滤波特点
- 仅在**空间域**滤波，无时间延迟
- 设计用于**高速运动场景**（如蹦床运动员）
- 可通过按 `f` 键实时开关

---

## 3. 删除内容详细列表

### 3.1 变量声明
```cpp
// 已删除
bool enable_bilateral_filter = true;
```
**原位置**: main() 函数内，数据记录控制变量之前

### 3.2 滤波处理代码块
**原位置**: 主循环中，从深度队列获取数据之后、处理图像之前
**删除行数**: 约 17 行

### 3.3 控制台提示信息
```cpp
// 已删除
std::cout << "  f       : Toggle bilateral filter (ON/OFF)" << std::endl;
```
**原位置**: Controls 输出部分

### 3.4 界面滤波状态显示
```cpp
// 已删除
std::ostringstream status_str;
status_str << "Filter: " << (enable_bilateral_filter ? "ON" : "OFF");
cv::putText(display, status_str.str(), cv::Point(10, 50),
            FONT_FACE, 1.3, enable_bilateral_filter ? cv::Scalar(0, 255, 0) : cv::Scalar(0, 0, 255), 2);
```
**原位置**: 显示循环中，设置显示之后

### 3.5 键盘 'f' 键处理逻辑
```cpp
// 已删除
} else if (key == 'f' || key == 'F') {
    // Toggle bilateral filter
    enable_bilateral_filter = !enable_bilateral_filter;
    std::cout << "Bilateral filter: " << (enable_bilateral_filter ? "ON" : "OFF") << std::endl;
}
```
**原位置**: 键盘事件处理部分

### 3.6 CSV 记录字段
```cpp
// CSV 头部 - 已删除 filter_enabled 字段
// 原: "frame,timestamp_ms,person_id,filter_enabled,cam_x,cam_y,cam_z,new_x,new_y,new_z\n"
// 新: "frame,timestamp_ms,person_id,cam_x,cam_y,cam_z,new_x,new_y,new_z\n"

// 数据行 - 已删除 enable_bilateral_filter 输出
// 原: csv_file << frame_count << "," << timestamp << "," << hip.person_id << "," << enable_bilateral_filter << "," ...
// 新: csv_file << frame_count << "," << timestamp << "," << hip.person_id << "," ...
```

### 3.7 region 窗口标题文字
```cpp
// 原: cv::putText(im, "=== Hip Coordinates (Depth Filtered) ===", ...);
// 新: cv::putText(im, "=== Hip Coordinates ===", ...);
```

---

## 4. 修改后的键盘控制

| 按键 | 功能 |
|------|------|
| q / ESC | 退出程序 |
| b | 切换边界框显示 |
| k | 切换关键点显示 |
| s | 切换骨架显示 |
| i | 切换信息覆盖层 |
| l | 切换数据记录 (开始/停止) |
| SPACE | 保存当前帧 |

**注**: `f` 键功能已移除

---

## 5. CSV 输出格式变化

### 5.1 原格式 (10 列)
```
frame,timestamp_ms,person_id,filter_enabled,cam_x,cam_y,cam_z,new_x,new_y,new_z
```

### 5.2 新格式 (9 列)
```
frame,timestamp_ms,person_id,cam_x,cam_y,cam_z,new_x,new_y,new_z
```

---

## 6. 影响说明

### 6.1 功能影响
- 深度数据不再经过双边滤波处理
- 深度噪声可能增加
- 处理速度略有提升（减少滤波计算开销）

### 6.2 兼容性
- CSV 文件格式变化，旧版数据分析脚本可能需要调整
- 界面显示简化，移除滤波状态指示

### 6.3 性能
- 移除 `cv::bilateralFilter()` 调用，减少 CPU/GPU 负载
- 移除深度数据的类型转换操作 (CV_16U -> CV_32F -> CV_16U)

---

## 7. 相关文档

以下 README 文档中可能包含对滤波功能的描述，建议后续检查更新：

- `1114噪声和滤波分析.md`
- `1114高速运动滤波配置.md`
- `1114滤波对比和数据分析功能实现.md`
- `1114深度图双边滤波实现.md`
- `1114EMA滤波实现说明.md`

---

## 8. 回滚方法

如需恢复滤波功能，可参考 Git 历史记录或本文档中记录的原代码进行恢复。

```bash
# 查看文件修改历史
git log --oneline get_pose_indemind_left.cpp

# 查看具体修改内容
git diff HEAD~1 get_pose_indemind_left.cpp
```
