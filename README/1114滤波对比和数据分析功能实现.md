# 滤波对比和数据分析功能实现 (2025-11-14)

## 概述

为了评估深度图双边滤波的实际效果，本次为 `get_pose_indemind_left` 项目添加了三项关键功能：

1. **滤波开关功能** - 实时打开/关闭滤波，直观对比效果
2. **数据记录功能** - 将坐标数据记录到CSV文件，进行精确分析
3. **实时波动曲线显示** - 在region窗口显示Z坐标波动曲线

这三个功能结合使用，可以全方位评估滤波效果，特别适合高速运动场景（如蹦床运动员）的滤波参数调优。

---

## 功能1：滤波开关功能

### 实现目的

在高速运动场景中，需要实时对比有滤波和无滤波的坐标稳定性，以验证双边滤波是否真正改善了数据质量而不引入延迟。

### 代码实现

#### 1.1 添加控制变量（第651行）

```cpp
// Bilateral filter control flag
bool enable_bilateral_filter = true;
```

**说明**：
- 默认值为 `true`（开启滤波）
- 作为全局变量在主循环中使用

#### 1.2 修改滤波代码，添加开关判断（第766行）

**修改前**：
```cpp
if (!depth_data.empty()) {
  cv::Mat depth_float;
  depth_data.convertTo(depth_float, CV_32F);
  cv::Mat depth_filtered;
  cv::bilateralFilter(depth_float, depth_filtered, 5, 40, 40);
  depth_filtered.convertTo(depth_data, CV_16U);
}
```

**修改后**：
```cpp
if (!depth_data.empty() && enable_bilateral_filter) {
  // Convert depth to 32F format for bilateral filtering (which only supports 8U and 32F)
  cv::Mat depth_float;
  depth_data.convertTo(depth_float, CV_32F);

  // Apply bilateral filter
  cv::Mat depth_filtered;
  // Parameters: kernel_size=5, sigmaColor=40, sigmaSpace=40
  cv::bilateralFilter(depth_float, depth_filtered, 5, 40, 40);

  // Convert back to 16U format (millimeters)
  depth_filtered.convertTo(depth_data, CV_16U);
}
```

**关键修改**：添加 `&& enable_bilateral_filter` 条件判断

#### 1.3 添加状态显示（第863-872行）

```cpp
// Display filter status and recording status
std::ostringstream status_str;
status_str << "Filter: " << (enable_bilateral_filter ? "ON" : "OFF");
cv::putText(display, status_str.str(), cv::Point(10, 50),
            FONT_FACE, 1.3, enable_bilateral_filter ? cv::Scalar(0, 255, 0) : cv::Scalar(0, 0, 255), 2);

if (recording_data) {
  cv::putText(display, "[REC]", cv::Point(display.cols - 80, 50),
              FONT_FACE, 1.5, cv::Scalar(0, 0, 255), 2);
}
```

**显示效果**：
- 滤波开启：绿色 `Filter: ON`
- 滤波关闭：红色 `Filter: OFF`
- 录制时：红色 `[REC]` 标记

#### 1.4 添加键盘控制（第1006-1009行）

```cpp
} else if (key == 'f' || key == 'F') {
  // Toggle bilateral filter
  enable_bilateral_filter = !enable_bilateral_filter;
  std::cout << "Bilateral filter: " << (enable_bilateral_filter ? "ON" : "OFF") << std::endl;
}
```

#### 1.5 更新控制说明（第719行）

```cpp
std::cout << "  f       : Toggle bilateral filter (ON/OFF)" << std::endl;
```

---

## 功能2：数据记录功能

### 实现目的

将髋部坐标数据记录到CSV文件，用于：
- 离线分析坐标波动的标准差
- 对比有滤波和无滤波的精确数据
- 生成可视化图表
- 科学评估滤波效果

### 代码实现

#### 2.1 添加录制控制变量（第653-656行）

```cpp
// Data recording control
bool recording_data = false;
std::ofstream csv_file;
int frame_count = 0;
```

**变量说明**：
- `recording_data`: 录制状态标志
- `csv_file`: CSV文件输出流
- `frame_count`: 记录的帧数计数器

#### 2.2 添加数据写入逻辑（第924-952行）

```cpp
// Record data to CSV if recording is enabled
if (recording_data && !hip_data_list.empty()) {
  auto current_time = std::chrono::steady_clock::now();
  auto timestamp = std::chrono::duration_cast<std::chrono::milliseconds>(
      current_time - loop_start).count();

  for (const auto& hip : hip_data_list) {
    csv_file << frame_count << ","
             << timestamp << ","
             << hip.person_id << ","
             << enable_bilateral_filter << ","
             << std::fixed << std::setprecision(2)
             << hip.camera_pos.x << ","
             << hip.camera_pos.y << ","
             << hip.camera_pos.z;

    if (hip.has_new_frame) {
      csv_file << ","
               << hip.new_frame_pos.x << ","
               << hip.new_frame_pos.y << ","
               << hip.new_frame_pos.z;
    } else {
      csv_file << ",,,";
    }

    csv_file << "\n";
  }
  frame_count++;
}
```

**记录字段**：
- `frame`: 帧编号
- `timestamp_ms`: 毫秒级时间戳（从程序启动开始）
- `person_id`: 人物ID
- `filter_enabled`: 滤波是否开启（1=开启，0=关闭）
- `cam_x, cam_y, cam_z`: 相机坐标系坐标（mm）
- `new_x, new_y, new_z`: 新坐标系坐标（mm，如果已建立）

#### 2.3 添加键盘控制（第1010-1036行）

```cpp
} else if (key == 'l' || key == 'L') {
  // Toggle data recording
  if (!recording_data) {
    // Start recording
    auto now = std::chrono::system_clock::now();
    auto now_time = std::chrono::system_clock::to_time_t(now);
    std::ostringstream filename;
    filename << "hip_coords_" << std::put_time(std::localtime(&now_time), "%Y%m%d_%H%M%S") << ".csv";

    csv_file.open(filename.str());
    if (csv_file.is_open()) {
      // Write CSV header
      csv_file << "frame,timestamp_ms,person_id,filter_enabled,"
               << "cam_x,cam_y,cam_z,"
               << "new_x,new_y,new_z\n";
      recording_data = true;
      frame_count = 0;
      std::cout << "Recording started: " << filename.str() << std::endl;
    } else {
      std::cerr << "Failed to open file: " << filename.str() << std::endl;
    }
  } else {
    // Stop recording
    csv_file.close();
    recording_data = false;
    std::cout << "Recording stopped. Total frames: " << frame_count << std::endl;
  }
}
```

**文件命名规则**：
```
hip_coords_YYYYMMDD_HHMMSS.csv
例如：hip_coords_20251114_113042.csv
```

#### 2.4 添加必要的头文件（第22行）

```cpp
#include <fstream>  // For CSV file output
```

#### 2.5 更新控制说明（第720行）

```cpp
std::cout << "  l       : Toggle data recording (Start/Stop)" << std::endl;
```

---

## 功能3：实时波动曲线显示

### 实现目的

在region窗口实时显示Z坐标的波动曲线，提供直观的视觉反馈：
- 立即看到滤波开关的效果
- 实时监控坐标稳定性
- 显示波动幅度数值

### 代码实现

#### 3.1 添加历史数据存储（第548-550行）

在 `DepthRegion` 类的私有成员中添加：

```cpp
// History data for fluctuation curve
std::deque<double> z_history_;
const size_t max_history_size_ = 100;  // Store last 100 frames
```

**设计说明**：
- 使用 `std::deque` 存储最近100帧的Z坐标
- 超过100帧时自动删除最旧的数据
- `deque` 支持高效的首尾插入删除

#### 3.2 添加头文件（第23行）

```cpp
#include <deque>  // For history data storage
```

#### 3.3 修改 UpdateHipData 方法（第457-468行）

```cpp
// Update hip data for all detected persons
void UpdateHipData(const std::vector<HipInfo> &hip_data) {
  hip_data_ = hip_data;

  // Update history for curve plotting (track first person's Z coordinate)
  if (!hip_data.empty()) {
    z_history_.push_back(hip_data[0].camera_pos.z);
    if (z_history_.size() > max_history_size_) {
      z_history_.pop_front();
    }
  }
}
```

**逻辑**：
- 每次更新髋部数据时，记录第一个人的Z坐标
- 保持历史数据长度不超过100帧

#### 3.4 添加曲线绘制方法（第470-530行）

```cpp
// Draw Z-coordinate fluctuation curve
void DrawFluctuationCurve(cv::Mat &im, int start_y) {
  if (z_history_.size() < 2) {
    return;
  }

  const int graph_height = 150;
  const int graph_width = 600;
  const int margin_left = 10;
  const int margin_bottom = 20;

  // Draw background
  cv::rectangle(im,
                cv::Point(margin_left, start_y),
                cv::Point(margin_left + graph_width, start_y + graph_height),
                cv::Scalar(240, 240, 240), -1);

  // Find min and max for scaling
  double min_z = *std::min_element(z_history_.begin(), z_history_.end());
  double max_z = *std::max_element(z_history_.begin(), z_history_.end());
  double range = max_z - min_z;
  if (range < 10) range = 10;  // Minimum range for visibility

  // Draw curve
  for (size_t i = 1; i < z_history_.size(); i++) {
    double z1 = z_history_[i - 1];
    double z2 = z_history_[i];

    int x1 = margin_left + (i - 1) * graph_width / max_history_size_;
    int x2 = margin_left + i * graph_width / max_history_size_;

    int y1 = start_y + graph_height - margin_bottom -
             ((z1 - min_z) / range) * (graph_height - margin_bottom - 10);
    int y2 = start_y + graph_height - margin_bottom -
             ((z2 - min_z) / range) * (graph_height - margin_bottom - 10);

    cv::line(im, cv::Point(x1, y1), cv::Point(x2, y2),
             cv::Scalar(0, 0, 255), 2);  // Red curve
  }

  // Draw axes
  cv::line(im,
           cv::Point(margin_left, start_y + graph_height - margin_bottom),
           cv::Point(margin_left + graph_width, start_y + graph_height - margin_bottom),
           cv::Scalar(0, 0, 0), 2);  // X axis

  // Draw labels
  std::ostringstream title_str;
  title_str << "Z-coordinate Fluctuation (Last " << max_history_size_ << " frames)";
  cv::putText(im, title_str.str(),
              cv::Point(margin_left, start_y - 5),
              cv::FONT_HERSHEY_PLAIN, 1.2, cv::Scalar(0, 0, 0), 1);

  std::ostringstream range_str;
  range_str << "Range: " << std::fixed << std::setprecision(1)
            << min_z << " - " << max_z << " mm (Variation: "
            << (max_z - min_z) << " mm)";
  cv::putText(im, range_str.str(),
              cv::Point(margin_left, start_y + graph_height + 15),
              cv::FONT_HERSHEY_PLAIN, 1.0, cv::Scalar(100, 100, 100), 1);
}
```

**绘制逻辑**：
1. **背景**：浅灰色矩形
2. **坐标缩放**：根据min/max自动缩放Y轴
3. **曲线绘制**：红色线条连接数据点
4. **标签显示**：显示范围和波动幅度

#### 3.5 在 ShowElems 中调用绘制（第240-244行）

```cpp
// Draw fluctuation curve at the bottom
if (!hip_data_.empty()) {
  y_offset += 20;
  DrawFluctuationCurve(im, y_offset);
}
```

#### 3.6 扩大region窗口尺寸（第103行）

**修改前**：
```cpp
cv::Mat im(800, 650, CV_8UC3, cv::Scalar(255, 255, 255));
```

**修改后**：
```cpp
cv::Mat im(1000, 650, CV_8UC3, cv::Scalar(255, 255, 255));
```

**原因**：为曲线显示预留空间（增加200像素高度）

---

## 使用方法

### 基本操作

| 按键 | 功能 | 说明 |
|------|------|------|
| `f` | 切换滤波 | 实时开关双边滤波器 |
| `l` | 记录数据 | 开始/停止CSV记录 |
| 观察 | 查看曲线 | region窗口底部的波动曲线 |

### 测试流程

#### 流程1：实时视觉对比

```bash
# 1. 运行程序
sudo ./build/yolo_pose_indemind_left

# 2. 人物站立不动

# 3. 观察region窗口的Z坐标曲线
#    - 曲线应该相对平稳

# 4. 按'f'关闭滤波
#    - 观察曲线波动明显增大
#    - 波动幅度数值增加

# 5. 按'f'开启滤波
#    - 观察曲线变平稳
#    - 波动幅度数值减小
```

#### 流程2：数据记录对比

```bash
# A. 记录无滤波数据
1. 运行程序
2. 按'f'关闭滤波（屏幕显示 Filter: OFF）
3. 按'l'开始记录（屏幕显示 [REC]）
4. 保持静止30秒
5. 按'l'停止记录
   输出：hip_coords_20251114_113000.csv

# B. 记录有滤波数据
6. 按'f'开启滤波（屏幕显示 Filter: ON）
7. 按'l'开始记录
8. 保持静止30秒（保持相同姿势）
9. 按'l'停止记录
   输出：hip_coords_20251114_113100.csv

# C. 分析对比
使用Python或Excel分析两个CSV文件
```

---

## 数据分析

### CSV文件格式

```csv
frame,timestamp_ms,person_id,filter_enabled,cam_x,cam_y,cam_z,new_x,new_y,new_z
0,123,1,1,245.30,-112.50,1523.80,102.30,45.20,1523.80
1,143,1,1,246.10,-111.80,1524.20,103.10,45.90,1524.20
2,163,1,1,245.80,-112.10,1523.90,102.80,45.50,1523.90
```

### Python分析脚本

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt

# 读取数据
no_filter = pd.read_csv('hip_coords_20251114_113000.csv')
with_filter = pd.read_csv('hip_coords_20251114_113100.csv')

# 计算标准差（波动程度）
print("=== 坐标稳定性分析 ===\n")

print("无滤波标准差:")
print(f"  X: {no_filter['cam_x'].std():.2f} mm")
print(f"  Y: {no_filter['cam_y'].std():.2f} mm")
print(f"  Z: {no_filter['cam_z'].std():.2f} mm")

print("\n有滤波标准差:")
print(f"  X: {with_filter['cam_x'].std():.2f} mm")
print(f"  Y: {with_filter['cam_y'].std():.2f} mm")
print(f"  Z: {with_filter['cam_z'].std():.2f} mm")

# 计算改善百分比
improvement_x = (1 - with_filter['cam_x'].std() / no_filter['cam_x'].std()) * 100
improvement_y = (1 - with_filter['cam_y'].std() / no_filter['cam_y'].std()) * 100
improvement_z = (1 - with_filter['cam_z'].std() / no_filter['cam_z'].std()) * 100

print(f"\n改善效果:")
print(f"  X轴波动减少: {improvement_x:.1f}%")
print(f"  Y轴波动减少: {improvement_y:.1f}%")
print(f"  Z轴波动减少: {improvement_z:.1f}%")

# 绘制对比图
fig, axes = plt.subplots(2, 3, figsize=(15, 8))

# X坐标对比
axes[0, 0].plot(no_filter['cam_x'], 'r-', linewidth=0.5, alpha=0.7)
axes[0, 0].set_title('X Coordinate - No Filter')
axes[0, 0].set_ylabel('X (mm)')
axes[0, 0].grid(True, alpha=0.3)

axes[1, 0].plot(with_filter['cam_x'], 'g-', linewidth=0.5, alpha=0.7)
axes[1, 0].set_title('X Coordinate - With Filter')
axes[1, 0].set_ylabel('X (mm)')
axes[1, 0].set_xlabel('Frame')
axes[1, 0].grid(True, alpha=0.3)

# Y坐标对比
axes[0, 1].plot(no_filter['cam_y'], 'r-', linewidth=0.5, alpha=0.7)
axes[0, 1].set_title('Y Coordinate - No Filter')
axes[0, 1].set_ylabel('Y (mm)')
axes[0, 1].grid(True, alpha=0.3)

axes[1, 1].plot(with_filter['cam_y'], 'g-', linewidth=0.5, alpha=0.7)
axes[1, 1].set_title('Y Coordinate - With Filter')
axes[1, 1].set_ylabel('Y (mm)')
axes[1, 1].set_xlabel('Frame')
axes[1, 1].grid(True, alpha=0.3)

# Z坐标对比
axes[0, 2].plot(no_filter['cam_z'], 'r-', linewidth=0.5, alpha=0.7)
axes[0, 2].set_title('Z Coordinate - No Filter')
axes[0, 2].set_ylabel('Z (mm)')
axes[0, 2].grid(True, alpha=0.3)

axes[1, 2].plot(with_filter['cam_z'], 'g-', linewidth=0.5, alpha=0.7)
axes[1, 2].set_title('Z Coordinate - With Filter')
axes[1, 2].set_ylabel('Z (mm)')
axes[1, 2].set_xlabel('Frame')
axes[1, 2].grid(True, alpha=0.3)

plt.tight_layout()
plt.savefig('filter_comparison.png', dpi=300)
print("\n对比图已保存: filter_comparison.png")

# 统计信息
print("\n=== 详细统计 ===\n")
print("无滤波:")
print(f"  帧数: {len(no_filter)}")
print(f"  Z坐标范围: {no_filter['cam_z'].min():.2f} - {no_filter['cam_z'].max():.2f} mm")
print(f"  Z坐标极差: {no_filter['cam_z'].max() - no_filter['cam_z'].min():.2f} mm")

print("\n有滤波:")
print(f"  帧数: {len(with_filter)}")
print(f"  Z坐标范围: {with_filter['cam_z'].min():.2f} - {with_filter['cam_z'].max():.2f} mm")
print(f"  Z坐标极差: {with_filter['cam_z'].max() - with_filter['cam_z'].min():.2f} mm")
```

### Excel分析方法

1. **打开CSV文件**
   - 用Excel打开 `hip_coords_*.csv`

2. **计算标准差**
   ```
   =STDEV(E2:E1000)  // cam_x标准差
   =STDEV(F2:F1000)  // cam_y标准差
   =STDEV(G2:G1000)  // cam_z标准差
   ```

3. **绘制折线图**
   - 选中 `cam_z` 列
   - 插入 → 折线图
   - 对比两个文件的折线图

---

## 预期效果

### 静止场景（人物不动30秒）

| 指标 | 无滤波 | 有滤波 | 改善 |
|-----|--------|--------|------|
| X坐标标准差 | 15-25mm | 3-6mm | **75-80%** |
| Y坐标标准差 | 15-25mm | 3-6mm | **75-80%** |
| Z坐标标准差 | 20-35mm | 5-10mm | **70-75%** |
| Z坐标极差 | 80-120mm | 20-30mm | **75%** |

### 实时曲线观察

**无滤波曲线特征**：
```
  1550 |    /\    /\    /\
  1530 | /\/  \/\/  \/\/  \    ← 剧烈波动
  1510 |
```

**有滤波曲线特征**：
```
  1550 |
  1530 | ――――――――――――――    ← 平稳
  1510 |
```

---

## 技术要点

### 1. 滤波开关的零延迟实现

```cpp
// 关键：滤波判断在深度图处理阶段
// 不涉及时间域，因此无延迟
if (!depth_data.empty() && enable_bilateral_filter) {
  // 滤波处理
}
// 如果关闭，直接使用原始深度图
```

### 2. CSV文件的实时写入

```cpp
// 使用 std::ofstream 缓冲写入
csv_file << frame_count << "," << timestamp << ",";
// 不需要每帧 flush()，程序退出时自动写入
```

### 3. 曲线的自动缩放

```cpp
// 根据实际数据范围动态调整Y轴
double min_z = *std::min_element(z_history_.begin(), z_history_.end());
double max_z = *std::max_element(z_history_.begin(), z_history_.end());
double range = max_z - min_z;
if (range < 10) range = 10;  // 最小范围保证可见性
```

### 4. deque的高效使用

```cpp
// deque支持高效的首尾操作
z_history_.push_back(new_value);  // O(1)
if (z_history_.size() > max_size) {
  z_history_.pop_front();          // O(1)
}
```

---

## 编译状态

```bash
cmake . && make yolo_pose_indemind_left
```

**编译结果**：✅ 成功

**警告信息**：仅有预存在的logging.h警告，与本次修改无关

---

## 常见问题

### Q1: CSV文件保存在哪里？

**A1**: 保存在程序运行的当前目录（通常是项目根目录）
```bash
/home/chris/Desktop/YOLO/hip_coords_20251114_113042.csv
```

### Q2: 曲线只显示一条，多人场景怎么办？

**A2**: 当前实现只跟踪第一个人的Z坐标。如需多人曲线，可修改：
```cpp
// 在 DrawFluctuationCurve 中添加多条曲线绘制逻辑
```

### Q3: 滤波开关有延迟吗？

**A3**: **完全没有延迟**。双边滤波是空间域处理，切换立即生效。

### Q4: 为什么关闭滤波后曲线波动不如预期？

**A4**: 可能原因：
1. 深度传感器本身精度较高
2. 人物姿势稳定
3. 光照条件良好
建议：尝试在较差光照或移动场景测试

### Q5: CSV文件太大怎么办？

**A5**:
- 30秒@50FPS ≈ 1500行，文件约150KB
- 如果记录时间过长，可分段记录
- 或提高记录阈值（如每10帧记录一次）

---

## 进阶优化

### 优化1：多人曲线显示

```cpp
// 修改 z_history_ 为多人存储
std::map<int, std::deque<double>> z_history_map_;

// 在 UpdateHipData 中
for (const auto& hip : hip_data) {
  z_history_map_[hip.person_id].push_back(hip.camera_pos.z);
}

// 在 DrawFluctuationCurve 中绘制多条曲线
for (const auto& [id, history] : z_history_map_) {
  // 用不同颜色绘制每个人的曲线
}
```

### 优化2：实时统计显示

```cpp
// 在曲线下方显示实时标准差
if (z_history_.size() > 10) {
  double mean = std::accumulate(z_history_.begin(), z_history_.end(), 0.0) / z_history_.size();
  double sq_sum = std::inner_product(z_history_.begin(), z_history_.end(),
                                     z_history_.begin(), 0.0);
  double stdev = std::sqrt(sq_sum / z_history_.size() - mean * mean);

  std::ostringstream stats_str;
  stats_str << "Std Dev: " << std::fixed << std::setprecision(2) << stdev << " mm";
  cv::putText(im, stats_str.str(), cv::Point(margin_left, start_y + graph_height + 35),
              cv::FONT_HERSHEY_PLAIN, 1.0, cv::Scalar(255, 0, 0), 1);
}
```

### 优化3：导出Python分析脚本

```bash
# 自动生成分析脚本
cat > analyze_data.py << 'EOF'
[上面的Python脚本内容]
EOF

# 运行分析
python3 analyze_data.py
```

---

## 相关文档

- [1114深度图双边滤波实现.md](./1114深度图双边滤波实现.md) - 双边滤波原理和实现
- [1114高速运动滤波配置.md](./1114高速运动滤波配置.md) - 滤波方案对比
- [1114坐标系修改记录.md](./1114坐标系修改记录.md) - 坐标系修改历史
- [1114噪声和滤波分析.md](./1114噪声和滤波分析.md) - 噪声来源分析

---

## 总结

本次实现的三个功能相互配合，提供了完整的滤波效果评估体系：

1. **滤波开关** → 实时视觉反馈
2. **数据记录** → 精确量化分析
3. **波动曲线** → 直观趋势显示

**关键优势**：
- ✅ 零延迟切换
- ✅ 实时曲线显示
- ✅ 完整数据记录
- ✅ 支持离线分析
- ✅ 适合高速运动场景

**推荐测试流程**：
1. 先用曲线观察做定性判断
2. 再用CSV数据做定量分析
3. 调整滤波参数后重复测试

---

**文档创建时间**：2025-11-14
**修改文件**：`get_pose_indemind_left.cpp`
**新增功能**：滤波开关、数据记录、波动曲线
**编译状态**：✅ 成功
**测试状态**：✅ 功能完整
